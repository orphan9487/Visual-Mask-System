# Generated output

- `current/` receives all new generation and experiment results.
- `archive/2026-08-pre-cleanup/` contains results created before the output
  directory was reorganized.

Generated files and the local archive are intentionally ignored by Git. Keep
only an empty `current/.gitkeep` in version control so the runtime output
location exists in a fresh checkout.
