# Visual Mask System — 參賽作品待辦盤點

目標：從「強技術 demo」→「完整可交件參賽作品」。
標記：工作量 S(小/<1h)、M(中/半天)、L(大/1天+)；負責 = 你(感知/推理/迴圈) / 隊友(生成) / 共同。

---

## P0　阻斷性——不補就是半成品（評審一眼看穿）

### 1. 生成迴路補完：真面具取代色塊　【共同，L】
目前 LINE 回的是色塊，7 情緒只有 anger/neutral 是真圖。這是「完整度」最大鴻溝。
- [ ] **1a. 前置：確認隊友生成層現況**（能跑？品質？介面吃不吃 VisualInstruction？）
- [ ] 1b. 預先渲染 7 情緒 × person8692 個人化素材（用你的 VisualInstruction 驅動 main.py 生成管線）
- [ ] 1c. 用 CLIP-T（evaluate_consistency）自動挑每情緒最佳素材
- [ ] 1d. LINE 改送真素材，移除 placeholder（app.py / assets.py）
- [ ] 1e. video modality：現有 GIF→mp4 轉檔，用 line_api.video_message() 送動態面具

### 2. 端到端整合：拔掉所有 mock　【共同，M】
- [ ] main.py:91-100 的寫死 decision → 改消費真實 VisualInstruction（介面合約已定）
- [ ] 確認「一則訊息 → 感知 → 推理 → 生成 → 送達」全程無假資料

---

## P1　交件與展示的必要條件

### 3. 穩定部署（demo 當天不掉線）　【你，M】
- [ ] 確認核心推論與生成服務可穩定啟動
- [ ] 一鍵啟動腳本與服務健康檢查
- [ ] 開機/斷線自動重啟（Windows 排程或 supervisor）

### 4. 參賽材料包　【你，M】
- [ ] README（給評審）：作品簡介、架構圖、如何跑、demo 步驟
- [ ] 架構圖（四層 + 資料流 + 你/隊友分工）
- [ ] Demo 展示影片/GIF（錄「真好啊」語境反轉、emoji 補回情緒兩個亮點）
- [ ] 技術亮點一頁（三層驗證數據、多模態、語境相依——你已有的實驗）

### 5. 對話記憶持久化　【你，S】
- [ ] Contextual Buffer 目前純記憶體、重啟即失 → 落地到檔案/SQLite（重啟不失憶）

---

## P2　健壯性與完成度

### 6. 錯誤處理與邊界情況　【你，M】
- [ ] LLM 推理失敗/逾時的 fallback（目前會讓事件處理拋錯）
- [ ] 並發：模型單例加鎖，多人同時發訊會排隊 → 至少確認不會崩、或加佇列
- [ ] 長對話：Contextual Buffer 過長 → 接 summarizer.py 摘要壓縮（感知層既有但未整合）
- [ ] 非文字/貼圖訊息（圖片、語音、位置）目前忽略 → 至少友善回覆而非靜默

### 7. 程式清理（交付前該做）　【你，S】
- [ ] 刪孤兒/重複檔：根目錄 `a`、src/generation/identity_manager.py、src/learning/train_lora.py、src/reasoning/mcot_decoder.py（用 Llama 非 TinyLlama、與現行機制不一致）
- [x] 修正 `tests/manual_verify_mask.py` 的輸出路徑
- [ ] reasoning_engine.py（舊版）已被 ERC 引擎取代 → 確認 main.py 改用新的後移除或標記 deprecated
- [ ] README 說明各模組職責

### 8. 學習迴圈 Feedback Monitor（補完你的架構）　【你，M】
- [ ] LINE 已部署，可做輕量版：記錄使用者對回饋的反應（如對貼圖的回覆、表情）
- [ ] 存成可分析的 log（未來 M-IT/RLHF 的資料基礎）
- 註：計畫已定位閉環 RLHF 為未來工作，此處只做「監控與蒐集」即可宣稱迴圈成形

---

## P3　加分項（有餘力再做）

- [ ] 回覆除了圖片，附上情緒標籤/短 caption，讓判讀結果可見
- [ ] Bot 加入群組時的 onboarding 訊息（說明用途）
- [ ] 多 identity：讓不同使用者綁不同面具（Identity-DB 已支援，缺多組 LoRA）
- [ ] /debug 端點在正式部署關閉或加保護

---

## 關鍵路徑（到「完整」的最短路線）

```
1a 確認隊友生成 → 1b-1e 真素材+動態 → 2 拔 mock   ← 這條完成，作品就「完整」
        ↓
3 穩定部署 + 4 材料包                              ← 這條完成，作品「可交件展示」
        ↓
5/6/7 健壯性清理                                   ← 加分、降低 demo 風險
```

## 你可獨立推進 vs 需隊友協調

- **需隊友（生成層）**：1a-1e、2 的生成端
- **你可獨立做**：3 部署、4 材料、5 持久化、6 健壯性、7 清理、8 學習迴圈
