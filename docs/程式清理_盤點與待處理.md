# 程式清理盤點（2026-07-24）

## 已刪除（我範圍內、確認被取代、無人 import）

| 檔案 | 原因 | 取代者 |
|---|---|---|
| `a`（根目錄） | 名為「a」的散落資料前處理腳本，誤存 | — |
| `src/reasoning/mcot_decoder.py` | 早期 SemanticIntentDecoder（Llama-3.2-1B、自由文字 M-CoT） | `src/reasoning/intent_decoder.py` + `erc_engine.py` |
| `src/perception/integrator.py` | ContextualBuffer 舊版 + 未用的 M_ICL_Integrator stub | 對話歷史改由呼叫端傳入；M-ICL few-shot 尚未實作 |
| `reasoning_engine.py`（根目錄） | 舊版推理引擎，已無任何 import | `src/services/emotion_service.py` + `src/reasoning/erc_engine.py` |
| `layers/` | 與 `src/` 重複的早期四層原型，已無任何 import | `src/perception/`、`src/reasoning/`、`src/generation/`、`src/learning/` |

已驗證：刪除後所有核心模組 import 正常。

## 已完成——生成層整理

| 檔案 | 狀態 | 建議 |
|---|---|---|
| `main.py` | 已改用 `PipelineService` 消費 VisualInstruction | 保留為 CLI smoke test |
| `tests/manual_verify_mask.py` | 生成 LoRA 驗證腳本（貓臉 mask）；輸出路徑問題已修正 | 保留作為手動生成驗證 |
| `src/generation/identity_manager.py` | 廢棄原型 | 已刪除，由 `src/reasoning/identity_db.py` 取代 |
| `src/learning/train_lora.py` | 放錯目錄的殘缺 stub | 已刪除，正式訓練工具集中於 `scripts/training/` |

## 保留的現行元件

| 檔案 | 狀態 | 建議 |
|---|---|---|
| `src/perception/summarizer.py` | Text Summarization 元件（BART），功能正常但未整合進主流程 | 保留。長對話摘要（健壯性待辦 6）可整合它 |
| `scripts/training/preprocess_data.py` | 資料前處理腳本，獨立、無人 import | 已歸類至訓練工具 |

## 後續

舊推理與生成原型已清除。後續新增通訊平台時，只在 `src/interfaces/`
新增 adapter，核心情緒與生成流程維持由 `src/services/pipeline_service.py` 統一協調。
