# 程式清理盤點（2026-07-24）

## 已刪除（我範圍內、確認被取代、無人 import）

| 檔案 | 原因 | 取代者 |
|---|---|---|
| `a`（根目錄） | 名為「a」的散落資料前處理腳本，誤存 | — |
| `src/reasoning/mcot_decoder.py` | 早期 SemanticIntentDecoder（Llama-3.2-1B、自由文字 M-CoT） | `src/reasoning/intent_decoder.py` + `erc_engine.py` |
| `src/perception/integrator.py` | ContextualBuffer 舊版 + 未用的 M_ICL_Integrator stub | 對話歷史改由呼叫端傳入；M-ICL few-shot 尚未實作 |

已驗證：刪除後所有核心模組 import 正常。

## 標記——生成層（隊友負責），我未觸碰

| 檔案 | 狀態 | 建議 |
|---|---|---|
| `main.py` | 生成主程式，仍寫死 mock decision | 由隊友改為消費 VisualInstruction（見介面合約） |
| `tests/manual_verify_mask.py` | 生成 LoRA 驗證腳本（貓臉 mask）；輸出路徑問題已修正 | 保留作為手動生成驗證 |
| `src/generation/identity_manager.py` | 廢棄原型（純 SD、指向不存在路徑），無人 import | 隊友確認後可刪 |
| `src/learning/train_lora.py` | 訓練「視覺面具 LoRA」（SD UNet）的殘缺 stub，其實屬生成層、放錯目錄 | 隊友決定；應移到 generation/ 或刪 |

## 標記——我範圍內、被引用或屬架構元件，保留

| 檔案 | 狀態 | 建議 |
|---|---|---|
| `reasoning_engine.py`（根目錄） | 舊版推理引擎，**仍被 main.py import**（`from reasoning_engine import MCoTReasoningEngine`）。已被 erc_engine 取代 | 待 main.py 改用新推理層後再移除——**需與隊友協調**，現在刪會弄壞 main.py |
| `src/perception/summarizer.py` | Text Summarization 元件（BART），功能正常但未整合進主流程 | 保留。長對話摘要（健壯性待辦 6）可整合它 |
| `scripts/training/preprocess_data.py` | 資料前處理腳本，獨立、無人 import | 已歸類至訓練工具 |

## 後續（需與隊友協調的清理）

1. main.py 改用 VisualInstruction → 之後可移除 `reasoning_engine.py`
2. `src/learning/train_lora.py` 移至 `src/generation/` 或刪除（它是生成 LoRA 訓練，非 ERC）
3. `src/generation/identity_manager.py` 廢棄原型，隊友確認後刪除
